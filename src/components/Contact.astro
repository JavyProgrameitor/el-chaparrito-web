---
import { site } from "../data/site";
const waLink = `https://wa.me/${site.whatsappPhone}?text=${encodeURIComponent("Hola, me gustaría pedir presupuesto y disponibilidad.")}`;
---

<section class="text-center text-jd-green font-bold mb-10">
  <h2>Contacto</h2>
  <p><strong>{site.name}</strong></p>
  <p>{site.address}</p>
  <p>
    📞 <a href={`tel:+34${site.phone1}`}>+34 {site.phone1}</a> ·{" "}
    <a href={`tel:+34${site.phone2}`}>+34 {site.phone2}</a>
  </p>
  <p>✉️ <a href={`mailto:${site.email}`}>{site.email}</a></p>
  <p>💬 <a href={waLink}>Abrir WhatsApp</a></p>
</section>
