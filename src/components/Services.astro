---
import { services } from "../data/services";
---

<section class="bg-jd-yellowSoft">
  <div class="mx-auto max-w-5xl px-4 py-14 sm:py-16">
    <div class="mx-auto max-w-3xl text-center">
      <h2 class="text-3xl font-black tracking-tight text-jd-green sm:text-4xl">Servicios</h2>
      <p class="mt-4 text-jd-greenDark font-bold">
        Siembra de precisión, cosecha y mapas de rendimiento para mejorar la toma de decisiones.
      </p>
    </div>

    <div class="mt-10 grid gap-4 sm:gap-6 md:grid-cols-2">
      {services.map((s) => (
        <article class="text-center group rounded-2xl border border-slate-200 bg-jd-yellow p-6 shadow-xl hover:-translate-y-0.5 hover:shadow-2xl">
          <h3 class="text-base font-semibold text-jd-greenDark">{s.title}</h3>
          <p class="mt-2 text-sm text-jd-green">{s.desc}</p>
          <div class="mt-4 h-1 w-10 rounded-full bg-brand-600/70 group-hover:w-14"></div>
        </article>
      ))}
    </div>
  </div>
</section>
